numpy==2.5.1
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.18.0
matplotlib==3.11.0
gymnasium==1.3.0
openpyxl==3.1.5
torch==2.12.1

# torch is CPU-only in this project's environment; install it separately if this
# plain `pip install -r requirements.txt` pulls a CUDA build you don't want:
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cpu
